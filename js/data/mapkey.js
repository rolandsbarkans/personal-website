/* Public browser keys. Both are visible to visitors by design and are secured
   by domain, not secrecy. Leave empty to run without them. */

/* Stadia Maps, for the watercolour tiles. Empty falls back to CARTO. */
window.STADIA_KEY = '0d6f5a32-3af6-4ec0-95fc-aa6796fb2a94';

/* Cloudflare Turnstile site key. Empty skips the bot check. */
window.TURNSTILE_SITE_KEY = '0x4AAAAAAEXm4YRYDoHMZaLx';
